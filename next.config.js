/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['fr', 'en', 'es', 'zh', 'de', 'it', 'ar', 'ja'],
    defaultLocale: 'fr',
  },
};

module.exports = nextConfig;
//test de synchro avec Github